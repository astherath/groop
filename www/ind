<html>
 <head>
 <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
 <script>
$(document).ready(function(){
    $('#submit').click(function(){
        var author = document.getElementById("authorField").value;
        var word = document.getElementById("wordField").value;
        var url2 = 'http://felipearce.pw:3000/graph?author=' + author + '&word=' + word;
        const Url='https://jsonplaceholder.typicode.com/posts';
        $.ajax({
            url: Url,
            type: "GET",
            success: function(result){
                alert(result);
            },
            error:function(error){
                alert(`error: ${error}`);
            }
        });
//        $.getJSON(Url, function(result) {
//            alert(result);
//        });
    });
});
 </script>
 </head>
 <body>
<input type="text" id="authorField" placeholder="Author">
<input type="text" id="wordField" placeholder="Word">
<button id="submit" onclick="runFunction()">Submit</button>


 </body>
</html>

